# Dependencias que SOLO sirven para correr las pruebas y los generadores.
# El sitio en producción no necesita nada de esto.
#
#   pip install -r api/requirements-dev.txt

-r requirements.txt

# TestClient de FastAPI habla HTTP contra la app en memoria y necesita httpx.
# FastAPI no lo trae por defecto: sin esto, api/test_api.py truena al importar.
httpx>=0.27

# Generadores de imágenes: favicon, retrato y tarjetas sociales.
Pillow>=10.0

# Modelo de precios en Excel.
openpyxl>=3.1
